---
phase: 05-muscle-list
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/components/mobile/MobileMuscleList.tsx
autonomous: true

must_haves:
  truths:
    - "Muscle groups render as collapsible sections with headers"
    - "Tapping a group header toggles its expanded/collapsed state"
    - "Chevron icon rotates to indicate expansion state"
  artifacts:
    - path: "src/ui/components/mobile/MobileMuscleList.tsx"
      provides: "Collapsible muscle group structure"
      exports: ["MobileMuscleList"]
      min_lines: 60
  key_links:
    - from: "src/ui/components/mobile/MobileMuscleList.tsx"
      to: "@core/taxonomy"
      via: "import UI_MUSCLE_GROUPS"
      pattern: "import.*UI_MUSCLE_GROUPS.*from.*taxonomy"
---

<objective>
Create collapsible muscle group structure for mobile muscle list

Purpose: Establish the expandable/collapsible UI pattern for displaying 26 muscles organized into 7 anatomical groups, using the proven WeeklyGoalEditor pattern.

Output: MobileMuscleList component with group headers that expand/collapse on tap.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-muscle-list/05-RESEARCH.md

Key patterns from existing code:
- WeeklyGoalEditor.tsx: Uses useState<Set<string>> for expanded groups, chevron rotation animation
- MobileHeatmap.tsx: Mobile component isolation pattern, loading/error states
- UI_MUSCLE_GROUPS from @core/taxonomy: 7 groups (Back, Chest, Shoulders, Arms, Legs, Core, Forearms)

Note: LIST-01 specifies 6 groups but UI_MUSCLE_GROUPS has 7. Per research recommendation, use existing 7 groups for consistency with WeeklyGoalEditor.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MobileMuscleList with collapsible groups</name>
  <files>src/ui/components/mobile/MobileMuscleList.tsx</files>
  <action>
Create MobileMuscleList.tsx in the mobile components directory.

Structure:
1. Import UI_MUSCLE_GROUPS from @core/taxonomy
2. Create interface MobileMuscleListProps with profileId: string | null and optional daysBack: number
3. Implement expanded state using useState<Set<string>> pattern from WeeklyGoalEditor
4. Start with first group expanded only (mobile-optimized: less initial scrolling)
5. Create toggleGroup function that toggles group membership in the Set
6. Render each group from UI_MUSCLE_GROUPS with:
   - Button header containing chevron + group name
   - Chevron rotates 90deg when expanded (transition-transform class)
   - Conditional content rendering when isExpanded
   - Placeholder text in content area (data wiring in Plan 02)
7. Use touch-appropriate styling:
   - :active pseudo-class for tap feedback (not :hover per MOBILE-02)
   - Scoped styles similar to MobileHeatmap pattern
   - bg-primary-800 for headers, bg-primary-900 for content

Export the component as named export.
  </action>
  <verify>
- `npm run lint` passes
- `npm run build` succeeds
- Component file exists at src/ui/components/mobile/MobileMuscleList.tsx
- Run `grep -l "UI_MUSCLE_GROUPS" src/ui/components/mobile/MobileMuscleList.tsx` returns the file
  </verify>
  <done>
MobileMuscleList component renders 7 collapsible groups. Tapping headers toggles expand/collapse. Chevron animates rotation. First group starts expanded, rest collapsed.
  </done>
</task>

</tasks>

<verification>
- Component exports MobileMuscleList function
- UI_MUSCLE_GROUPS is imported from @core/taxonomy
- useState<Set<string>> manages expanded state
- Each group header is a button with onClick handler
- Chevron has rotate-90 class when expanded
- Content area conditionally renders based on expanded state
- No lint errors or type errors
</verification>

<success_criteria>
- Component structure complete with 7 collapsible groups
- Expand/collapse interaction works on tap
- Chevron rotation animation present
- Mobile touch feedback with :active styling
- Ready for Plan 02 to add progress bars and data
</success_criteria>

<output>
After completion, create `.planning/phases/05-muscle-list/05-01-SUMMARY.md`
</output>
