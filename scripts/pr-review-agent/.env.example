# PR Review Agent Handler Configuration

# Port for the webhook handler server
PORT=3847

# Webhook secret (must match GitHub Actions secret PR_REVIEW_WEBHOOK_SECRET)
# Generate with: openssl rand -hex 32
WEBHOOK_SECRET=your-webhook-secret-here

# Anthropic API Key (for Claude Agent SDK)
ANTHROPIC_API_KEY=your-anthropic-api-key

# Optional: Database path (defaults to ./data/agent-sessions.db)
# DB_PATH=/path/to/agent-sessions.db
