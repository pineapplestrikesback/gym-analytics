# PR Review Agent Handler Configuration (Vercel)

# Webhook secret (must match GitHub Actions secret PR_REVIEW_WEBHOOK_SECRET)
# Generate with: openssl rand -hex 32
WEBHOOK_SECRET=your-webhook-secret-here

# Anthropic API Key (for Claude API)
ANTHROPIC_API_KEY=your-anthropic-api-key

# Vercel KV (automatically set when you link a KV store in Vercel dashboard)
# KV_REST_API_URL=https://your-kv-instance.kv.vercel-storage.com
# KV_REST_API_TOKEN=your-kv-token
# KV_REST_API_READ_ONLY_TOKEN=your-kv-read-only-token
